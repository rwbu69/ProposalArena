---
description: Add and run tests for domain logic, storage, AI provider adapters, and critical browser journeys.
---
# Testing

Inspect the existing test setup first. Prioritize deterministic tests for interview transitions and serialization. Add AI fixtures for malformed JSON and provider failures. Browser-test critical flows such as adding a proposal, configuring a provider, starting an interview, submitting an answer, and generating a report.

Report exactly which commands were run and whether they passed.
